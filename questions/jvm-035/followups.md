## 追问 1：为什么要打破双亲委派？Tomcat为什么要自己搞一套类加载器？

答：Tomcat需要支持在同一个容器里部署多个Web应用，每个应用可能依赖同一个库的不同版本。

如果严格遵循双亲委派，所有应用都用同一个类加载器加载同一个类，只能有一个版本，不同应用就冲突了。

所以Tomcat给每个Web应用配一个独立的WebAppClassLoader，加载顺序是：先自己在WEB-INF下找类加载，找不到再向上委派。这样每个应用可以有自己独立的类版本，实现应用之间的隔离。
