## 追问 1：对象一定都在堆上分配吗？有没有例外？

答：不一定。开启逃逸分析后，有两种情况对象不在堆上：

1. **栈上分配**：对象不逃逸出方法，直接在栈上分配，方法结束自动销毁

2. **标量替换**：更激进，对象被拆成字段直接存在局部变量表里，连对象结构都没有了

这两种都是JIT的优化，目的是减少堆上对象数量，减轻GC压力。

另外还有一种特殊情况：**字符串常量池里的字符串对象**，虽然也在堆里（Java7+），但是是存在字符串常量池的特殊区域管理的。
