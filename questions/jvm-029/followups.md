## 追问 1：什么是热点代码？JVM怎么判断一段代码是不是热点？

答：热点代码就是运行频率很高的代码，主要分两类：被多次调用的方法、被多次执行的循环体。

判断方式是**热点探测**，HotSpot用的是**计数器法**：每个方法有调用计数器，每调用一次+1，超过阈值就认为是热点方法，触发JIT编译。

还有一种是**采样法**：周期性检查栈顶，哪个方法经常出现在栈顶就是热点。采样法简单但不精确，HotSpot不用。
