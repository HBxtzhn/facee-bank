## 追问 1：Java8为什么把永久代改成元空间？好处是什么？

答：核心原因有三个：

1. **避免OOM**：永久代有固定大小上限，很容易因为加载类过多而OOM；元空间用本地内存，默认只受系统物理内存限制

2. **解耦JVM和类元数据**：永久代大小调优困难，元空间可以动态扩容

3. **融合HotSpot和JRockit**：JRockit本来就没有永久代，Oracle收购后统一设计

但注意元空间不是无限的，可以通过`-XX:MaxMetaspaceSize`限制上限，防止物理内存被耗尽。
