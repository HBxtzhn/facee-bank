## 追问 1：为什么推荐自增ID做主键？

聚簇索引按主键顺序物理排列。自增ID新数据总是追加到B+树最右侧不引起页分裂和数据移动。UUID随机值可能插入已满中间页触发页分裂造成性能下降和空间碎片。自增ID作为二级索引叶子节点值更小(8字节 vs UUID 16字节+)。

## 追问 2：用pgvector做向量检索和B+树索引有什么不同？

B+树适用一维等值/范围查询。向量是高维数据(1536维)无法用B+树有效索引。pgvector使用IVFFlat(聚类)或HNSW(多层图)近似最近邻算法——针对高维空间相似度检索而非一维排序。
