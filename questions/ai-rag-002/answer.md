纯向量检索有三个致命问题:
1. **精确匹配差**: 像"SKU-2026-001"这种产品编号，向量检索效果差
2. **专业术语召回低**: 低频专业术语在embedding空间中信号弱
3. **专有名词容易丢**: 人名/地名/公司名等

混合检索 = 向量检索(语义) + 关键词检索(BM25，精确)。两路结果通过RRF(Reciprocal Rank Fusion)融合:
```
RRF_score(doc) = sum(1/(k + rank_i))  # k通常取60
```
纯向量检索在精确匹配场景失败率高达30%，加BM25后降到5%以下。这是2026年生产环境标配。
