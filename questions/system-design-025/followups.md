## 追问 1：对象转换怎么做？

手写get/set最直接; MapStruct编译期生成转换代码性能接近手写; BeanUtils.copyProperties基于反射不推荐。关键字段转换要显式写防止自动映射搞混。

## 追问 2：什么情况下DTO和VO可以合并？

请求和响应字段完全一致时——简单查询接口GET /tickets/{id}的返回直接作详情VO。但后期修改返回字段应立刻拆分VO。判断标准: 输入输出字段结构是否独立变化。
