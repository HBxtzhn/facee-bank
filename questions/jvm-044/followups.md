## 追问 1：元空间就不会OOM了吗？什么情况下元空间也会溢出？

答：元空间也会OOM，只是概率低了。

两种情况会触发元空间OOM：

1. 设置了-XX:MaxMetaspaceSize上限，加载的类元数据超过这个值

2. 没设上限，但操作系统物理内存+交换区被耗尽了

典型场景还是动态生成大量类：比如CGLIB生成大量代理类、OSGi模块化频繁重载类、应用服务器部署几十个应用。只是因为元空间上限更高了，所以更难触发而已。
