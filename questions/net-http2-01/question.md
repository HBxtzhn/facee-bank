# What happens when you type a URL into a browser?

Walk through what happens, end to end, when you type `https://example.com` into a browser and press Enter. Keep it structured and mention where caches can short-circuit the flow.
