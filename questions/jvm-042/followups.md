## 追问 1：可达性分析的时候需要STW吗？为什么？

答：需要STW，至少标记的开始和结束节点需要暂停。

因为如果用户线程一直在跑，对象引用关系一直在变，分析结果就不准了——刚标记完存活的对象，下一秒引用就断了；或者刚标记为垃圾的对象，又被重新引用了。

所以可达性分析必须在一个一致性的快照上进行。传统的Serial、Parallel收集器是全程STW；CMS、G1这些并发收集器是初始标记和最终标记STW，中间的并发标记阶段和用户线程一起跑，配合写屏障解决并发修改问题。
