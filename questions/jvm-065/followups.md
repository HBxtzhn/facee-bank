## 追问 1：为什么生产环境建议-Xms和-Xmx设成一样大？

答：主要两个原因：

1. **避免扩容开销**：如果Xms < Xmx，程序运行中堆不够了需要动态扩容，扩容要向操作系统申请内存、调整JVM内部结构，有性能开销。设成一样大，启动时就申请好全部内存，运行中不用扩容。

2. **避免内存抖动**：堆内存会在最小值和最大值之间伸缩，频繁扩容缩容影响性能。设成固定值，内存稳定。

生产环境一般都是固定堆大小，性能更稳定，也方便评估内存占用。开发环境可以设小一点启动快。
