## 追问 1：消费者处理某条消息失败后续怎么处理？

关键不能简单nack让消息回到队列头部会阻塞后续。应nack但requeue=false发到死信队列(DLX)，不阻塞其他消息。专门死信处理消费者重试或记录异常。配置死信队列: `x-dead-letter-exchange`参数。

## 追问 2：什么情况下可以不关心消息顺序？

大多数场景——用户上传文档做embedding、发短信验证码、批量邮件通知，每条完全独立先后不影响最终结果。只有状态机流转、库存扣减、转账等才需关心顺序。设计时尽量让消息独立化不引入不必要顺序依赖。
