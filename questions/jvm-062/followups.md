## 追问 1：ZGC的停顿时间为什么和堆大小没关系？

答：因为ZGC的STW阶段只处理GC Roots直接引用的对象，不遍历整个对象图。

初始标记只标记GC Roots直接指向的对象，数量就那么多，和堆多大没关系。
重定位开始阶段也只移动GC Roots直接引用的对象，然后把剩下的对象移动放到并发阶段做。

堆越大，并发阶段的时间越长，但并发不影响用户线程，STW时间还是那么短。所以不管堆是8G还是8TB，ZGC的STW都是毫秒级的。

这是ZGC最厉害的地方——传统收集器堆越大STW越长，ZGC的STW几乎恒定。
