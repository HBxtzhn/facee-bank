## 追问 1：String s = new String\("abc"\) 创建了几个对象？

答：分两种情况：

如果字符串常量池里已经有"abc"了：只创建1个对象——堆里的new String对象。

如果常量池里还没有"abc"：创建2个对象——先在常量池里创建一个"abc"字符串对象，再在堆里创建一个new String对象，new String内部的char数组引用常量池里的那个。

注意：字符串常量池存的是对象引用还是对象本身，不同JDK版本实现有差异，但面试按2个/1个的经典答法就可以。
