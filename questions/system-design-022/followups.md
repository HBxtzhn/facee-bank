## 追问 1：令牌桶和漏桶限流的区别？

漏桶强制平滑——不管流入速度，处理速度恒定，突发流量一律排队或丢弃。令牌桶允许一定突发——令牌积累够瞬时突发流量可处理。需严格匀速用漏桶(对外API按QPS计费)，允许突发用令牌桶(秒杀开始瞬间流量激增)。

## 追问 2：限流在哪个层面做？Gateway还是应用层？

两个层面都要做但职责不同。Gateway做总控防恶意流量冲垮系统; 应用层做精细粒度针对不同接口/用户等级设不同阈值。只Gateway可能路径匹配不精确漏掉攻击，只在应用层则请求已进业务容器浪费了连接和线程资源。
