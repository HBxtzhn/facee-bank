三步: 1) 即时止血——Gateway/Nginx限流兜底，检查Redis缓存命中率; 2) 排查原因——日志看异常(爬虫/恶意攻击/正常峰值/上游响应慢导致堆积); 3) 分级优化——短期限流+降级保证核心功能，长期优化缓存/水平扩展/读写分离+分库分表。

限流双层: Gateway(入口)做总控(Sentinel)，应用层做精细粒度(接口+用户级别)。降级是主动行为(关非核心保核心)，熔断是被动保护(下游故障率超阈值自动断掉返回fallback)。
