## 追问 1：MVCC下读不加锁写不阻塞读，对业务系统有什么好处？

高并发抢票场景: 大量用户查询剩余票数(读)同时有人下单(写)。MVCC让查询不等写锁释放，快照读立即返回，用户体验好吞吐高。不必为读性能牺牲写正确性。

## 追问 2：MVCC缺点？undo log会无限膨胀吗？

缺点: 额外存储、长事务导致旧版本无法清理、需额外purge线程。undo log不会无限膨胀有purge线程定期清理。但长事务不提交会使其对应ReadView中最老事务ID很老，导致之后所有undo版本无法清理。避免长事务。
