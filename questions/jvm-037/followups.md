## 追问 1：JIT编译的代码会被回收吗？什么时候回收？

答：会回收。Code Cache不是只增不减的。

当一个方法被标记为"不再是热点"（调用计数器衰减到阈值以下），或者它所属的类被卸载了，对应的编译代码就可以被回收。

Java7之后有Code Cache Flushing机制，Code Cache快满的时候会主动清理一些冷的、不常用的编译代码，腾出空间给新的热点代码。但清理也有开销，频繁刷Cache也会影响性能。
