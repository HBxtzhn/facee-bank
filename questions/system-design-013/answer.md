实际实现忘记密码功能时，还需要注意下面这些安全要求：

- 无论账号是否存在，都返回一致的提示，并尽量保持接近的响应时间，避免用户枚举。
- 重置令牌使用密码学安全随机数生成，具备足够熵，只能使用一次，并在较短时间后过期。
- 对重置请求和令牌校验进行限流；重置链接只使用可信域名和 HTTPS，避免令牌通过 Referer 泄露。
- 密码修改成功后发送安全通知，并根据风险使已有会话失效，或至少让用户能够一键注销其他会话。
