## 追问 1：Survivor区放不下存活对象了怎么办？

答：触发**分配担保机制**。

Young GC之前，JVM会检查老年代最大可用连续空间是否大于新生代所有对象总空间。如果大于，这次Young GC是安全的。

如果小于，就看HandlePromotionFailure参数是否允许担保失败。允许的话继续检查老年代最大可用空间是否大于历次晋升老年代对象的平均大小，大于就尝试Young GC；小于或者不允许担保，就改成Full GC。

真的GC后Survivor装不下，存活对象就直接通过分配担保进入老年代。
