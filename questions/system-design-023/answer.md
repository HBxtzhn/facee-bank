分层设计:
- **前端**: 静态页面CDN化、防重复点击、验证码分流
- **网关**: Nginx/SLB第一层限流
- **业务层(最重要)**: 库存预热到Redis，用Lua脚本原子扣减(`if decr >= 0 then return 1 else incr return 0 end`)。扣减成功后发MQ异步创建订单，订单服务慢慢消费写入数据库。数据库乐观锁兜底防超卖
- **隔离**: 秒杀独立部署不和日常业务混用连接池和线程池
