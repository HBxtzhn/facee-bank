## 追问 1：有了JIT之后Java能追上C\+\+的性能吗？差距在哪？

答：对于长时间运行的服务端程序，热点代码被C2深度优化后，Java的性能可以接近甚至在某些场景超过C++。因为JIT有运行时数据，可以做一些C++静态编译做不了的优化（比如基于运行时profile的分支预测优化）。

但整体还是有差距：

1. 启动预热期慢，刚启动时是解释执行

2. GC开销，需要额外CPU时间做垃圾回收

3. 对象头、内存对齐等带来的内存开销

4. 不是所有代码都能被JIT编译（比如反射、动态类加载）

峰值性能C++还是更优，但Java胜在开发效率和内存安全。
