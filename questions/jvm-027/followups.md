## 追问 1：TLAB是线程私有的，那GC的时候其他线程能看到TLAB里的对象吗？会不会漏标？

答：TLAB只是分配时的私有概念，物理上还是在Eden区里，属于堆的一部分。所有线程的TLAB都在同一个堆内存空间中，GC扫描的时候是扫描整个Eden区，所以完全看得到TLAB里的对象，不会漏标。

TLAB的"私有"只是分配权私有——只有持有它的线程可以在里面分配新对象，内存地址对所有线程都是可见的。
