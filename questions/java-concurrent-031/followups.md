## 追问 1：CPU密集和IO密集任务线程数怎么设？

CPU密集: 线程数=CPU核数+1，多了上下文切换降低效率。IO密集: CPU核数×2或CPU核数/(1-阻塞系数)，阻塞系数约0.8-0.9，大部分时间等IO可多开线程。

## 追问 2：AI调用场景用哪种拒绝策略？

CallerRunsPolicy。AI调用耗时长(数秒到数十秒)，线程池和队列都满时直接抛异常或丢弃任务会丢请求。CallerRunsPolicy让调用线程自己执行任务，虽然阻塞当前请求但至少不丢，自然形成背压。
