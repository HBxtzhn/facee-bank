## 追问 1：内存泄漏和内存溢出有什么区别和联系？

答：内存泄漏是原因，内存溢出是结果。

内存泄漏：对象已经没用了但被意外持有引用，GC收不掉。这时候内存还没满，程序还能跑，只是可用内存越来越少。

内存溢出（OOM）：内存真的不够用了，新对象分配不下来，抛出错误。

联系：内存泄漏积累到一定程度，最终就会导致内存溢出。但OOM不一定都是泄漏导致的，也可能是内存真的不够、对象太多、堆设太小了。

简单说：泄漏是慢刀子，一点点放血；溢出是最后一刀，撑不住了。
